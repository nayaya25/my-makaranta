name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: my_makaranta
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/my_makaranta?schema=public
      JWT_SECRET: ci-test-secret
      NODE_ENV: test

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.2

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Apply migrations + seed
        working-directory: apps/api
        run: |
          pnpm exec prisma migrate deploy
          pnpm exec prisma db seed

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit + component tests
        run: pnpm test

      - name: API integration tests (tenancy, RLS, auth)
        working-directory: apps/api
        run: pnpm test:e2e

      - name: Build
        run: pnpm build

      # Security audit per the dependency policy. Report-only for now: clearing the
      # remaining high-severity advisories needs major upgrades (Next 15, NestJS 11,
      # Vitest 3, Storybook 9). Flip to a hard gate (drop continue-on-error) after that pass.
      - name: Security audit (production deps)
        continue-on-error: true
        run: pnpm audit --prod --audit-level high

      # NOTE: per-package ESLint flat-config consolidation is a tracked follow-up;
      # typecheck + tests + build are the enforced gates here.
