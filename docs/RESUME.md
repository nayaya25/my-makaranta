# myMakaranta — Resume / Handoff

**For a fresh session.** Read this, then continue from "Next steps (in order)". Companion docs: `PRD-v1.md`, `System-Design-Document-v1.md`, `docs/DESIGN-SYSTEM.md`, `docs/superpowers/specs/2026-06-13-web-mvp-build-reconciliation-design.md` (the build contract — read it).

## What this is
Multi-tenant school-management platform for Nigerian secondary schools. Delivery = **responsive web + PWA** (native Expo deferred). Monorepo: pnpm + Turborepo.

## Stack & locked decisions
- **apps/api** NestJS 11 (Express 5) modular monolith · Prisma 5 · PostgreSQL · BullMQ + Redis.
- **apps/web** Next.js 15 + React 19 (App Router, PWA) — the authenticated app (all stakeholders, responsive).
- **apps/marketing** Next.js 15 static landing site.
- **packages/ui** in-house design system over Radix + Tailwind 3 + cva; Storybook 9. Design = "Bold Ink" base + "Saffron warmth" (see DESIGN-SYSTEM.md / memory).
- Providers (env-selected, never live in tests): Storage S3 / local-fs; Email Mailgun / log; SMS Termii / mock; Payments Paystack / mock.
- Multi-tenancy: `schoolId` + Prisma middleware (`TENANT_MODELS`) + PostgreSQL RLS (FORCE) + non-superuser `mymakaranta_app` role. Money = kobo integers. Permissions-as-primitive (`@RequirePermissions` + DB-resolved `PermissionGuard`). Auto-audit middleware. Phone-OTP→JWT with `tokenVersion` revocation.

## Local dev environment (IMPORTANT)
- **No Docker.** Postgres + Redis run natively. `apps/api/.env`: `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/my_makaranta?schema=public`, `REDIS_URL=redis://localhost:6379`.
- **Port 4000 is reserved on this Windows machine** (WinNAT excluded range 3941–4040). API runs on **4080** (`apps/api/.env` PORT=4080); web points at it via `apps/web/.env.local` `NEXT_PUBLIC_API_BASE_URL=http://localhost:4080`.
- Tests need NODE_ENV=test (jest sets it) + Postgres running. e2e: `cd apps/api && NODE_ENV=test pnpm exec jest --config ./test/jest-e2e.json`. Unit: `pnpm --filter @mymakaranta/api exec jest`. UI: `pnpm --filter @mymakaranta/ui exec vitest run`. Builds: `pnpm build`.

## Current state
- **main** = Sprints 0–2.5 + Sprint 3 s1–s4.5 merged. **Sprint 3 slice 5 (report-card PDF + public verification)** built + browser-QA'd on `sprint-3-report-card` (ready to merge).
- Sprint 3 slice 5 (2026-06-15): report-card PDF (print page) + public result verification. New **non-tenant** `Verification` model (NOT in TENANT_MODELS, NO RLS — the public path has no tenant context, so reading an RLS table would return nothing in prod) holding a per-`ResultSheet` minimal public snapshot keyed by a 16-char crypto `code` (`verification.util.generateVerificationCode`, Crockford-ish alphabet no 0/1/I/L/O). Created at release (`release.service`), refreshed on correction (`correction.service` re-rank loop upserts every affected sheet's snapshot), lazily created by the report-card endpoint for pre-slice-5 sheets. `report-card.service` (`GET /v1/assessment/report-card?studentId=&termId=`, `results.release`, tenant-scoped + IDOR) returns the frozen sheet + grade key + class size + the code. **First public endpoint:** `PublicModule` → `GET /v1/public/verify/:code` (NO guard, NO tenant) reads only `Verification` → `{valid, student, className, term, school, average, position, issuedAt}` (minimal; no entries/ids). Web: `/report-card/[studentId]?termId=` print page (letterhead + subject table + grade key + QR via `qrcode` + Save-as-PDF) + public `/verify/[code]` page (outside the `(app)` shell) + `Report card` entry per student on `/release`; `(app)` sidebar/topbar now `print:hidden`. 110 API e2e (full suite, 15 files; +1 release-Verification, +1 correction-refresh, +2 report-card incl. cross-tenant + idempotent, +2 public verify) + `generateVerificationCode` unit (2). Browser QA passed: report card renders + lazy code, public verify works with NO auth header (minimal fields, idempotent), bad code → not-found, page is standalone. Also fixed slice-4.5 OTP test isolation (reset `OtpRequest` in correction `beforeAll`). Spec+plan `docs/superpowers/{specs,plans}/2026-06-15-sprint-3-slice-5-*`.
- **Known follow-ups (slice 5):** the public `/v1/public/verify/:code` has no rate-limit/throttle (protected only by the unguessable code) — add throttling before prod; confirm prod CORS allows the public route for QR-scan contexts; optional school-logo letterhead.
- Sprint 3 slice 4.5 (2026-06-15): proprietor-signed correction of a released component score. New tenant-scoped `Correction` model (TENANT_MODELS + RLS FORCE + 2 migrations) capturing old/new value+total+position, reason, `otpVerified`, correctedBy. New `results.correct` permission (PROPRIETOR auto-grant; **backfilled** for existing proprietors). `School.requireCorrectionOtp Boolean @default(true)` — tenant-configurable step-up OTP. `AuthService.assertOtp(phone,code)` = single-use step-up re-verify (no JWT). `correction.service`: `correct()` (`results.correct`) → OTP gate when enabled + reason + value-range + tenant/release/IDOR validation → `$transaction` upserts the Score, recomputes the subject entry (create if previously unscored) + the student average + **re-ranks the whole class** (`computePositions`) + writes the `Correction`; `getConfig`/`setConfig` (`assessment.configure`) for the OTP flag; `getCorrectableScores` for the modal. Web: correction modal on `/release` (subject→component→value+reason+conditional OTP) + Settings → Assessment "Require OTP for corrections" toggle + api client. 104 API e2e (full suite; +6 correction incl. re-rank/audit/OTP-invalid/replay/reason/range/unreleased/cross-tenant, +3 config-flip/no-OTP/correctable, +2 assertOtp) . Browser QA passed BOTH modes (OTP-signed: Bola Exam→65, otpVerified=true, re-froze sheet; toggle off → no OTP field → CA2→9, otpVerified=false). Code-review fixes: truthful `otpVerified`, `schoolId`-scoped tx updates, dropped dead release check. Spec+plan `docs/superpowers/{specs,plans}/2026-06-15-sprint-3-slice-4_5-*`.
- **Known follow-up (slice 4.5):** OTP step-up is not user/purpose-bound (`OtpRequest` has no `userId`) — a login OTP for the same phone could be replayed as a correction OTP within its 10-min TTL. Phone is `@unique` + JWT-derived so cross-user is impossible; close fully later with a `userId`/`purpose` column on `OtpRequest`.
- Sprint 3 slice 4 (2026-06-15): release workflow + immutability. New models `Release`/`ResultSheet`/`ResultSheetEntry` (tenant-scoped: TENANT_MODELS + RLS FORCE + 2 migrations) — freeze-never-recompute. `release.service` (`results.release`): `release(classId,termId)` validates tenant ids → 409 if already released → gathers offered subjects + enrolled students + Scores → `computeSubjectResult` per (student,subject) → average = round(mean of scored offered-subject totals) → `computePositions` (pure helper, standard competition ranking 1-2-2-4) → one interactive `$transaction` creating Release + ResultSheet + entries (explicit `schoolId` on every create, since `tx` runs no middleware); `getStatus(termId)` (classes-with-enrolments + released flag, tenant-scoped via Class relation) + `getSheet(classId,termId)` (frozen rows ordered by position). **Immutability:** `ScoresService.saveScores` after `assertContext` → if a Release exists for (class,term,school) → 409 "Results released…". Web `/release` dashboard (term picker → per-class Release/View + frozen sheet table) + nav (Lock icon) + api client. Full API e2e = 14 suites / 93 tests (assessment suite +5: 4 release incl. tie + re-release 409 + status + cross-tenant 404, +1 immutability 409) + `computePositions` unit (3). Browser QA passed end-to-end (release → status flip → frozen sheet Ada P1 85/A1, Bola P2 8/F9 → gradebook edit blocked with surfaced 409). Code-review fixes applied: added missing `schoolId` scope on `subjectAssignment` read, closed `getStatus` cross-tenant over-read, added query-param guards. Spec+plan `docs/superpowers/{specs,plans}/2026-06-15-sprint-3-slice-4-*`.
- Sprint 3 slice 3 (2026-06-14): `anomaly.util` (flagAnomalies — population mean/σ/z, guards n<2 & σ=0) + `review` service (`GET /v1/assessment/review/class-master` + `subject-master`, `results.review`, compute-on-read, explicit scoping + IDOR) — class-master = students×subjects matrix (total/grade/anomaly + average); subject-master = per-class mean+drift vs subject mean/σ + per-student z/anomaly; anomaly cohort = per (subject,term) all classes. Web `/review` two-mode page. No new model; position still deferred to slice 4. 25 assessment e2e (+3 review incl. cross-tenant + deterministic >2σ) + anomaly.util unit. Browser QA passed (OUT z=−2.3 flagged, JSS2A drift +7.1 / JSS2B −10.7, API-matched). Spec+plan `docs/superpowers/{specs,plans}/2026-06-14-sprint-3-slice-3-*`.
- Sprint 3 slice 2 (2026-06-14): `Score` model (tenant + RLS) + `computeSubjectResult` (additive total + grade) + scores service (gradebook `GET /v1/assessment/scores` + batch `POST`, `results.record`, explicit schoolId scoping + value/enrollment/IDOR validation) + **structure guard** (block `PUT /assessment/types` once any score exists → 409). Web `/gradebook` grid (class/term/subject selectors, live total+grade per row, out-of-range flagged + save-blocked, empty states) + api client + `computeRow`. 19 assessment e2e (incl. cross-tenant + guard) + 22 web tests. Final review READY TO MERGE; browser QA passed (enter→live calc→save→persist; out-of-range blocked). Spec+plan `docs/superpowers/{specs,plans}/2026-06-14-sprint-3-slice-2-*`.
- Slice-2 follow-ups (non-blocking): blanking a saved cell doesn't clear it (omitted from batch, not zeroed) — decide delete-on-blank semantics in a later slice; `POST /scores` accepts empty `scores:[]` (no-op).
- Sprint 3 slice 1 (2026-06-14): new `apps/api/src/modules/assessment/` module — 3 tenant-scoped models (`AssessmentType`, `GradeBoundary`, `SubjectAssignment`) + RLS + `assessment.configure` permission; 3 API resources: assessment types (replace-as-unit, **sum-to-100**), grade boundaries (replace-as-unit + WAEC/NECO templates), subject assignments (per-year CRUD with tenant-IDOR validation); pure `resolveGrade`. Web: Settings → Assessment 3-panel config UI (grade bands w/ live preview, components w/ running-total gate, assignments w/ per-row teacher reassignment) + api client + `grade.ts` helper. 14 assessment e2e (incl. cross-tenant isolation) + 2 web grade tests. Final review: READY TO MERGE. Browser QA: all panels verified end-to-end (WAEC apply+save, types 100✓+save, assign/reassign/dup-409/remove). Spec+plan in `docs/superpowers/{specs,plans}/2026-06-14-sprint-3-slice-1-*`.
- **Remaining Sprint 3 slices (each its own spec→plan→build):** 6 reveal animation (Framer Motion). (Deferred: full unlock-window correction — slice 4.5 shipped targeted single-score correction only; report-card rate-limit/CORS hardening + logo letterhead — slice 5 follow-ups.)
- Sprint 2 browser QA (2026-06-13): fixed 1 high-sev seam bug — overview rate showed "1.0%" instead of "100%" (API `rate` is a 0–1 fraction; web treated it as 0–100). Fix `1bb3a83`.
- Sprint 2.5 (2026-06-14): offline-first attendance. New `apps/web/src/lib/offline/` layer — `idb`-backed mark `queue` (composite-key coalescing) + `roster-cache` + pure `overlay` + `syncer` (batch-by-(class,date) flush, backoff retry, online/offline listeners) + `useOfflineSync` hook; grid (`/attendance`) rewritten offline-first (every tap durably queued, optimistic UI, cache-fallback load + queued-mark overlay, offline pill + sync indicator). Replay-safe via the existing idempotent `(studentId,date)` upsert — **zero backend change**. Offline browser QA verified the full round trip (offline mark → no premature send → reconnect sync → server persisted) + cache-fallback render with API down. Spec/plan in `docs/superpowers/{specs,plans}/2026-06-14-*`.
- **apps/web has vitest** (added Sprint 2.5): **22 web tests** (18 offline + 2 grade + 2 gradebook). API e2e: 22-test assessment suite (config + scores + guard + review + cross-tenant) + anomaly.util/score.util/grade.util units. All builds + typecheck + lint green.
- **Tenancy learning (important, applied in the assessment module):** the Prisma `$use` middleware does NOT reliably inject the `schoolId` filter inside an array `$transaction` NOR in the service-level e2e/`TenantContext.run` pattern. **Explicitly scope every read/delete by `TenantContext.schoolIdOrThrow()` + `where:{schoolId}`** in services; don't trust the middleware for these. (Single-record `update`/`delete` by unique id stay scoped via a prior scoped `findFirst` + RLS, per `prisma.service.ts`.) Existing modules rely on the middleware and their tests pass, but new tenant-scoped services should scope explicitly. [[tenant-idor-rule]]
- Security audit: `pnpm audit --prod` → 0 critical, 6 high (all transitive `tar` via bcrypt build toolchain, runtime-unreachable, unfixable upstream). CI gates on critical.
- **Open follow-ups (non-blocking):** (a) wrap `db.close()` in try/finally in `queue.ts`/`roster-cache.ts` (handle-leak on a rejected put/get; low impact); (b) Service Worker Background Sync for sync-while-closed (deferred — iOS Safari unsupported); (c) wrap the offline `Badge` pill in an `aria-live` region.

## What's built (by area)
- Auth: OTP request/verify, JWT, tenant middleware (verifies JWT), PermissionGuard, tokenVersion revocation.
- SIS: schools onboarding (create→proprietor grant+fresh token), academic years/terms, class levels, classes, subjects; students (+profile, photo upload w/ signed URLs), staff, parents/guardians, enrollment.
- Bulk import: BullMQ worker (tenant-scoped, row-level errors) + web CSV/XLSX UI (papaparse + exceljs).
- Attendance (Sprint 2): AttendanceRecord (tenant + RLS), roster/mark(batch upsert, last-write-wins, idempotencyKey)/student-history/summary; web `/attendance` tap-to-cycle grid + `/attendance/overview` heatmap. Hardened against cross-tenant IDOR.
- Offline attendance (Sprint 2.5): `apps/web/src/lib/offline/` — IndexedDB queue/cache + syncer + hook; grid is offline-first (durable queue, cache-fallback read, reconnect sync). Client-only; backend unchanged.
- Assessment (Sprint 3 s1–s5): `apps/api/src/modules/assessment/` — config + `Score` + gradebook + `computeSubjectResult` + structure guard + `anomaly.util` + review service + release workflow (`Release`/`ResultSheet`/`ResultSheetEntry` + `computePositions` + immutability) + proprietor-signed correction (`Correction` + step-up OTP `assertOtp` + tenant `requireCorrectionOtp` + re-rank) + report-card read + non-tenant `Verification`. `apps/api/src/modules/public/` — first unauthenticated endpoint (`/v1/public/verify/:code`). Web: Settings → Assessment, `/gradebook`, `/review`, `/release` (+ correction modal + report-card link), `/report-card/[studentId]` print page, public `/verify/[code]`.
- Web: `(app)` shell + sidebar (Dashboard, Students, Staff, Classes, Attendance, Gradebook, Review, Release, Settings), login, onboarding wizard.

## Next steps (in order)
1. **Merge `sprint-3-report-card` → main** once satisfied (`git checkout main && git merge sprint-3-report-card --no-edit`), then branch **Sprint 3 slice 6 (reveal animation, Framer Motion)** — the final Sprint 3 slice.
2. **Sprint 3 slice 6** (reveal animation), its own spec→plan→build. Then Sprint 3 is complete.
3. **Clear standing follow-ups:** Sprint 2.5 — db.close try/finally, SW Background Sync, offline-pill aria-live. Sprint 3 slice 1 — release note: backfill `assessment.configure` for any PROPRIETOR created before this deploy (the auto-grant only fires at school creation).

### Browser-QA playbook (reusable; learned this pass)
- Seed via API (no web enrollment screen): `POST /auth/otp/request {phone}` → OTP prints in api.log mock SMS → `POST /auth/otp/verify {phone,code}` → `POST /v1/schools` (use the returned **fresh token** thereafter) → `/v1/academic-years` (term `isCurrent:true`; roster resolves term by the `isCurrent` flag, NOT by date, so calendar drift is fine) → `/v1/class-levels` → `/v1/classes` → `/v1/students` ×3 → `/v1/enrollments` {studentId,classId,termId}.
- **gstack browse on this Windows box restarts the daemon between separate Bash calls, wiping localStorage** → do each interaction sequence in ONE bash call. Web stores auth in `localStorage` keys `mm.token` + `mm.user`; re-inject them at the start of each call (capture once after a real login), then `goto`.
- React controlled inputs: set value via the native setter + dispatch `input`+`change` (plain `fill`/`.value=` doesn't trigger React state). Set dependent date inputs one at a time with a wait between (batching otherwise drops one).
- `browse screenshot`/`snapshot -o` saves relative to the daemon's cwd (often `apps/web/.gstack/...`) — `find` it and copy into the repo, and `rm -rf apps/web/.gstack` after.
- **`next build` poisons `next dev`'s `.next`:** running `pnpm --filter web build` while/before `next dev` uses the same `.next` makes the dev server serve HTML referencing chunks that 404 → blank white pages app-wide. Fix: stop dev, `rm -rf apps/web/.next`, restart `next dev`. During QA, stop the web dev server before any production `build`.
- `browse js` does NOT await promises (async IIFEs return undefined) — can't read IndexedDB directly; use the UI's own indicators (e.g. the sync `pendingCount`) as the observable proxy. To emulate offline without a network toggle: `Object.defineProperty(navigator,'onLine',{value:false,configurable:true})` + `dispatchEvent(new Event('offline'))` (and `'online'` to restore); to test real fetch failure, kill the API process (taskkill the :4080 PID).

## How the work has been run
Orchestrator builds the tenancy/security-critical core inline (TDD); parallel subagents build disjoint modules/screens; orchestrator owns integration (barrels, app.module wiring) + verification + commits. Every commit triggers a background security-review hook — address its findings. Three independent safety nets used: automated tests, the security-review hook, and live browser QA (each catches a different bug class).

## Key learnings (also in agent memory)
- **Tenant IDOR rule:** validate any request-supplied entity id through a tenant-scoped model before write/return — especially `upsert` (middleware doesn't scope its where) and models without a `schoolId` (Enrollment, Guardian). Recurred 3×.
- Dependency policy: latest stable + `pnpm audit`; majors as a verified pass (test suite as guardrail), not blind bumps.
- Design: Bold Ink + Saffron, tokens in `packages/ui/tokens.ts` are the single source of truth.
