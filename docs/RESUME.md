# myMakaranta — Resume / Handoff

**For a fresh session.** Read this, then continue from "Next steps (in order)". Companion docs: `PRD-v1.md`, `System-Design-Document-v1.md`, `docs/DESIGN-SYSTEM.md`, `docs/superpowers/specs/2026-06-13-web-mvp-build-reconciliation-design.md` (the build contract — read it).

## What this is
Multi-tenant school-management platform for Nigerian secondary schools. Delivery = **responsive web + PWA** (native Expo deferred). Monorepo: pnpm + Turborepo.

## Stack & locked decisions
- **apps/api** NestJS 11 (Express 5) modular monolith · Prisma 5 · PostgreSQL · BullMQ + Redis.
- **apps/web** Next.js 15 + React 19 (App Router, PWA) — the authenticated app (all stakeholders, responsive).
- **apps/marketing** Next.js 15 static landing site.
- **packages/ui** in-house design system over Radix + Tailwind 3 + cva; Storybook 9. Design = "Bold Ink" base + "Saffron warmth" (see DESIGN-SYSTEM.md / memory).
- Providers (env-selected, never live in tests): Storage S3 / local-fs; Email Mailgun / log; SMS Termii / mock; Payments Paystack / mock.
- Multi-tenancy: `schoolId` + Prisma middleware (`TENANT_MODELS`) + PostgreSQL RLS (FORCE) + non-superuser `mymakaranta_app` role. Money = kobo integers. Permissions-as-primitive (`@RequirePermissions` + DB-resolved `PermissionGuard`). Auto-audit middleware. Phone-OTP→JWT with `tokenVersion` revocation.

## Local dev environment (IMPORTANT)
- **No Docker.** Postgres + Redis run natively. `apps/api/.env`: `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/my_makaranta?schema=public`, `REDIS_URL=redis://localhost:6379`.
- **Port 4000 is reserved on this Windows machine** (WinNAT excluded range 3941–4040). API runs on **4080** (`apps/api/.env` PORT=4080); web points at it via `apps/web/.env.local` `NEXT_PUBLIC_API_BASE_URL=http://localhost:4080`.
- Tests need NODE_ENV=test (jest sets it) + Postgres running. e2e: `cd apps/api && NODE_ENV=test pnpm exec jest --config ./test/jest-e2e.json`. Unit: `pnpm --filter @mymakaranta/api exec jest`. UI: `pnpm --filter @mymakaranta/ui exec vitest run`. Builds: `pnpm build`.

## Current state
- **main** = Sprints 0–4 complete + Sprint 5 s1–s2 merged. **Sprint 5 slice 3 (smart alerts)** built + QA'd on `sprint-5-dashboard-alerts` (ready to merge → completes Sprint 5). **Sprint 5 (Reporting & Dashboards) COMPLETE** once s3 merges (slices 1, 2, 3).
- Sprint 5 slice 3 (2026-06-17): dashboard smart alerts — PRD "quiet alert" trend signals (no model/migration). New pure `apps/api/src/modules/dashboard/alerts.util.ts` — `Alert` type (`type`/`severity`/`classId`/`className`/`message`) + `ALERT_THRESHOLDS` + `formatNairaFromKobo` + `buildAlerts(inputs, opts)` with three per-class heuristics: **ATTENDANCE_DIP** (baseline − recent ≥0.10 with ≥10 recent marks; high ≥0.20), **LOW_COLLECTION** (overdueKobo>0; high if ≥30% of expected), **RESULTS_OVERDUE** (term ≥80% elapsed/ended + unreleased + scored<offered; high when ended). `DashboardService.getAlerts(termId?)` + `GET /v1/dashboard/alerts?termId=` (`reports.view`): batched, explicitly tenant-scoped reads build one `ClassAlertInput` per class — baseline + recent-7d attendance `groupBy`, per-class overdue via `computeInvoiceStatus` fold over enrollment, results coverage offered∩scored + released, termElapsedFraction — then `buildAlerts`. Same term resolution as s1/s2 (foreign 404; no current term → `alerts:[]`). Server builds the human `message` (incl. money); web `<AlertsPanel/>` (`apps/web/src/app/(app)/dashboard/alerts-panel.tsx`) is a dumb severity-toned renderer mounted on BOTH dashboards (proprietor above KPIs, principal above table), renders nothing when no alerts / on 403 (calm). 161 API e2e (24 files; +3 alerts: 4-class scenario dip/overdue/results/healthy, foreign-404, no-term) + `buildAlerts` unit (13 boundary tests). QA passed at HTTP level (proprietor → 3 high alerts dip 50% / ₦50k overdue 100% / results 1/2, healthy class none; 401 no-token; 404 foreign term). QA record `.gstack/qa-reports/2026-06-17-sprint5-3-dashboard-alerts.md`. Spec+plan `docs/superpowers/{specs,plans}/2026-06-17-sprint-5-slice-3-*`.
- Sprint 5 slice 2 (2026-06-17): principal operational dashboard — PRD story "who to call into my office today" (no model/migration). Extended `apps/api/src/modules/dashboard/` with `DashboardService.getPrincipalSummary(termId?)` + `GET /v1/dashboard/principal?termId=` (`reports.view`) + pure `feePaidRate(collected,expected)` (0 when expected 0). Returns `{ term, classes:[{ classId, className, formTeacher, attendance:{rate,presentDays,totalDays}, results:{subjectsScored,subjectsOffered,released}, fees:{expectedKobo,collectedKobo,paidRate} }] }` — **per-class**, all reads **batched** (one query per dimension over the term's classes, assembled in JS, no N+1) + **explicitly tenant-scoped**. Per-class: attendance `groupBy([classId,status])` scoped to the term's classes + window; results coverage = **offered ∩ scored** subject sets (so scored never exceeds offered — fixed in review; offered = `subjectAssignment` by academicYear, scored = distinct `Score.subjectId` by term) + released flag; fees folded per class via `enrollment` (studentId→classId, `@@unique([studentId,termId])` guarantees one class/student) → invoices summed. Form teacher resolved from `Class.formTeacherId`→`Staff`. Sorted by `classLevel.order` then name. Same term resolution as slice 1 (foreign termId 404; no current term → `classes:[]`). Web: role-aware `/dashboard` — non-PROPRIETOR staff with `schoolId` → `PrincipalDashboardView` (dense table: Class · Form teacher · Attendance% · Results `X/Y` + released/draft Badge · Fees paid% + ₦; `warning` tone on rows with attendance<85% or incomplete submission), **403 → quick-links-stub fallback** (`onForbidden` on `ApiError.status===403`, so lower-privilege staff aren't broken); proprietor keeps the slice-1 showpiece. 158 API e2e (24 files; +4 principal incl. per-class coverage/fees/sort, foreign-404, no-term, + the offered∩scored regression guard) + `feePaidRate` unit (3). Code-review hardening: coverage offered∩scored; tenant scoping of the unscoped-Enrollment query traced safe (classIds derive from schoolId-scoped classes). QA passed at HTTP level (proprietor via onboarding → JSS1A 8/10 + 2/3 released + 0.75 fees, JSS2A 0/0 + 0/2 draft + 1.0 fees, sorted; 401 no-token; 404 foreign term). QA record `.gstack/qa-reports/2026-06-17-sprint5-2-principal-dashboard.md`. Spec+plan `docs/superpowers/{specs,plans}/2026-06-17-sprint-5-slice-2-*`.
- Sprint 5 slice 1 (2026-06-17): proprietor dashboard — the PRD "selling room" (no model/migration). New `apps/api/src/modules/dashboard/` — two pure helpers (`attendanceRate` = (present+late)/total as a 0..1 fraction with zero-division guard; `pickTopClass` = highest mean, null-safe, deterministic on ties) + `DashboardService.getProprietorSummary(termId?)` composing **explicitly tenant-scoped** reads: fees via the pure `summarizeInvoices` util (slice 3c) + a `payment.aggregate` for collected-this-week; attendance `groupBy(status)` scoped to the term's classes (`classId in` the term's enrolled classes) over the window `[term.startDate, min(now, term.endDate)]`; results = `release` count vs the term's class count + `resultSheet.groupBy` `_avg(average)` for the top class. `GET /v1/dashboard/proprietor?termId=` (`reports.view`, seeded + proprietor-auto-granted — no new perm/backfill); `termId` optional → current term (`Term.isCurrent`); foreign termId → 404 (tenant-IDOR); no current term → `term:null` + zeroed KPIs. Web: role-aware `/dashboard` — `identityType === "PROPRIETOR"` → `ProprietorDashboardView` (term selector + KPI hero cards: collected-this-week hero, fees row Expected/Collected/Outstanding/Overdue, attendance %, results-release progress + top class; reuses the `/fees` card vocabulary + `formatMoney`); other staff keep the quick-links stub; parents redirect to `/parent`. 154 API e2e (24 files; +4 dashboard incl. current-term resolution, explicit-termId, foreign-term 404, no-term-null) + `attendanceRate`/`pickTopClass` unit (5). Code-review hardening: attendance scoped to the term's classes (not date-window alone — `AttendanceRecord` has no `termId`). QA passed at HTTP level (proprietor via onboarding → fees 12M/9M/3M/3M + this-week 6M, attendance 0.8 (8/10), results 1/1 topClass 80; 401 no-token; 404 foreign term). QA record `.gstack/qa-reports/2026-06-17-sprint5-1-proprietor-dashboard.md`. Spec+plan `docs/superpowers/{specs,plans}/2026-06-17-sprint-5-slice-1-*`.
- Sprint 4 slice 4b (2026-06-17): parent pay portal (no model/migration). API: `ParentService` gained child-ownership-gated `getInvoices`/`pay`/`payVerify` (`fees.pay.own`) — a shared private `childStudentIds(user)` resolves the linked Parent's `Guardian→Student` ids (tenant-scoped via `Parent {id, schoolId}` AND `guardian.student:{schoolId}`); any invoice/payment whose student isn't the parent's child → uniform **404** (no ownership leak, no Payment created on a rejected pay). `pay`/`payVerify` delegate to slice-2 `PaymentsService.initializeOnline`/`verifyPayment` after the gate (bursar endpoints stay `fees.manage`). `GET /v1/parent/invoices` returns `[{studentId, studentName, invoiceId, termLabel, totalKobo, paidKobo, balanceKobo, status (computeInvoiceStatus), dueDate}]`; `POST /v1/parent/pay {invoiceId, amountKobo, email}` → `{reference, authorizationUrl}`; `POST /v1/parent/pay/verify {reference}` → `{applied, status, receiptCode?}`. `ParentModule` imports `PaymentsModule`. Web: role-aware nav in `(app)/layout.tsx` (PARENT → only `[{Fees → /parent}]`, else the full staff `NAV_ITEMS`); `/dashboard` redirects a PARENT → `/parent`; new mobile-first `(app)/parent/page.tsx` portal (invoices grouped by child → status Badge + balance → Pay → amount input → `parentPay` → `window.open(authorizationUrl)` → "I've paid — confirm" → `parentPayVerify` → View-receipt link to `/receipt/[code]` + re-fetch). `AuthUser` already had `identityType`; `email` is NOT on it → the pay form reads it defensively and shows an email input fallback. 150 API e2e (23 files; +5 parent-pay incl. lists-only-children/init-PENDING/non-child-404-no-payment/idempotent-verify/cross-tenant). Code-review hardening (Task 1): tenant-scoped `childStudentIds` Guardian query (`student:{schoolId}`) + `fees.pay.own` on `GET /children`. QA passed at HTTP/contract level (real guard+DTO+routing): OTP→PARENT auto-link → invoices OVERDUE ₦75k → ownership 404 → DTO 400 → pay → verify applied (receiptCode) → re-verify noop → invoice PAID balance 0 → public receipt by code. Full browser automation skipped (gstack daemon friction on Windows; security crux server-side + HTTP-verified; web build+typecheck+lint clean + code-reviewed). QA record `.gstack/qa-reports/2026-06-17-sprint4-4b-parent-pay.md`. Spec+plan `docs/superpowers/{specs,plans}/2026-06-17-sprint-4-slice-4b-*`.
- Sprint 4 slice 4a (2026-06-17): parent identity link (auth foundation; no model/migration/UI). `AuthService.verifyOtp` → private `linkParentIfMatch`: a PENDING user whose phone matches **exactly one** `Parent` is claimed → `identityType=PARENT`, `identityId=Parent.id`, `schoolId`, `tokenVersion++`, granted `fees.pay.own`+`results.view.own`; 0/multi matches stay PENDING; never overrides PROPRIETOR/STAFF; idempotent (atomic conditional `updateMany where identityType=PENDING`); the elevation is audit-logged (best-effort). `identityId` now flows on the JWT payload + `JwtStrategy.validate` (re-reads user from DB) + `RequestUser`. `apps/api/src/modules/parent/` — `GET /v1/parent/children` (JwtAuthGuard; resolves the PARENT's `Guardian→Student` list, tenant-scoped via `Parent {id, schoolId}` → cross-tenant identityId → `[]`). 145 API e2e (22 files; +4 parent-link incl. zero/multi/PROPRIETOR-safe/idempotent, +3 parent-children incl. cross-tenant). Code-review hardening: perms-in-txn, atomic claim, audit. API QA passed (seeded Parent phone → OTP login → PARENT + schoolId → /parent/children = [Ada Eze] → /me PARENT → re-login idempotent). Spec+plan `docs/superpowers/{specs,plans}/2026-06-17-sprint-4-slice-4a-*`.
- Sprint 4 slice 3c (2026-06-16): fees finance summary (read-only; no new model). Pure `summarizeInvoices(rows, now)` (reuses `computeInvoiceStatus`) → `{expectedKobo, collectedKobo, outstandingKobo, overdueKobo, byClassLevel:[{classLevelName, expected/collected/outstanding, studentCount}]}` — unit 4. `finance.service.getFinanceSummary(termId)` (`reports.view`): loads the term's invoices → summarize + a `payment.aggregate` `_sum` of SUCCESS payments with `paidAt ≥ now−7d` & `invoice:{termId}` → `collectedThisWeekKobo`. `GET /v1/fees/finance/summary?termId=`. Web `/fees` gained a finance summary section (5 KPI cards: Expected/Collected/Outstanding/Overdue(red)/Collected-this-week + by-class-level table) above the collections table. 138 API e2e (20 files; +2 finance) + summarizeInvoices unit (4). Browser QA passed (Expected ₦120k = Collected, Outstanding/Overdue ₦0, this-week ₦120k, JSS1 2 students). Spec+plan `docs/superpowers/{specs,plans}/2026-06-16-sprint-4-slice-3c-*`.
- Sprint 4 slice 3b (2026-06-16): bank-CSV reconciliation (stateless — no new model/migration). Pure `reconcile.util` matcher (`scoreCandidate`/`matchRow`: name-token overlap of narration vs student name + admissionNo substring = primary; amount-vs-balance = secondary; confidence high/low/none; amount-only never suggests) — unit 8. `reconciliation.service` (`fees.manage`): `proposeMatches(termId, rows)` (read-only — loads outstanding invoices → ranked candidates + suggestion per bank row), `confirmMatches(confirmations, actor)` (per-row `PaymentsService.recordOfflinePayment(channel BANK_TRANSFER)`; dup reference → ConflictException caught → `skipped`; foreign invoice → NotFoundException → `errors[]`; never aborts the batch). Web `/fees` "Reconcile bank statement" section: upload CSV → papaparse (dynamic default import, mirrors student-import) + header auto-detect (amount/credit, narration/description, reference, date) → review table (suggested student + confidence chip + override `<select>`/skip + editable amount) → confirm → `{recorded,skipped,errors}` + reload. 136 API e2e (19 files; +5 reconciliation incl. ranking/confidence/confirm-apply/dup-skip/cross-tenant) + matcher unit (8). Browser QA passed (propose: Ada high / Bola high / gibberish none → confirm recorded 2 → both PAID → re-confirm skipped 1; UI section renders). FeesModule now imports PaymentsModule (+ the two existing fees/collections e2e specs import the @Global PaymentsProviderModule for DI). Spec+plan `docs/superpowers/{specs,plans}/2026-06-16-sprint-4-slice-3b-*`.
- Sprint 4 slice 3a (2026-06-16): overdue + reminders (collections core). `Invoice.dueDate DateTime?` (bulk-settable per term) + `FeeReminder` model (tenant + RLS). Pure `computeInvoiceStatus({totalKobo,paidKobo,dueDate,now}) → "UNPAID"|"PARTIAL"|"PAID"|"OVERDUE"` (LOCAL string-literal union — the schema's `InvoiceStatus` enum is referenced by no model so Prisma prunes it at runtime/type; derived on read, no stored column/cron) — unit 5. `collections.service`: `setDueDate(termId,date)` (bulk `updateMany`), `getCollections(termId)` (status + balance + dueDate + lastRemindedAt, sorted OVERDUE-first then balance desc), `sendReminder(invoiceId)` (fans out to the student's Guardians→Parents: SMS each phone + email each with an email, per-recipient failures non-fatal, logs a `FeeReminder` with recipientCount + channels), `sendBulkReminders(termId)`. `fees.manage`/`fees.view`. `FeesModule` now imports `EmailModule` (DI self-sufficiency) + already had AuthModule (SmsService). Web `/fees` table is now the collections view (status badges error/success/warning/neutral, due-date control, per-row Remind + Remind-all, last-reminded). 131 API e2e (18 files; +6 collections incl. OVERDUE/sort/reminder-fanout/settled-400/bulk/cross-tenant) + computeInvoiceStatus unit (5). Browser QA passed (set past due date → OVERDUE → remind Ada recipientCount 1 + last-reminded → remind-all 2/1). Spec+plan `docs/superpowers/{specs,plans}/2026-06-16-sprint-4-slice-3a-*`.
- Sprint 4 slice 2 (2026-06-16): payments + auto-reconcile + receipts (bursar-side). New `core/payments/` env-selected provider (`PAYMENT_SERVICE` token + `PaymentProvider` interface; `mock` + `paystack` adapters; `PAYMENTS_PROVIDER`, **fails closed in production** if not `paystack`; HMAC-SHA512 webhook signature, timing-safe). `Payment` (tenant + RLS, reuses enums `PaymentStatus`/`PaymentChannel`) + `Receipt` (NON-tenant, no RLS — public receipt path, mirrors slice-5 `Verification`). `payments.service`: `recordOfflinePayment` (CASH/BANK_TRANSFER → immediate apply, dup ref → 409), `initializeOnline` (PENDING + provider.initialize), `verifyPayment`, `handleWebhook` (no tenant; resolves schoolId from the payment), `getReceipt` (public), `getPayments`. **Idempotent reconcile** = `updateMany({reference, status:PENDING}→SUCCESS)` claim (row-lock; count 0 → no-op covers already-applied AND unknown ref) then `paidKobo += amountKobo` + Receipt; `Receipt.paymentId @unique` is a hard double-apply backstop. Applies `payment.amountKobo` (NOT the provider's reported amount). Partial + overpayment allowed (balance can go negative = credit). Public surface (no JWT): `POST /v1/public/payments/webhook` (raw body via `NestFactory rawBody:true`, 401 on bad sig) + `GET /v1/public/receipt/:code`. Web: `/fees` invoice detail gained Record-payment (channel + amount) + Pay-online (init → verify) + View-receipt; public printable `/receipt/[code]`. 125 API e2e (17 files; +6 payments incl. idempotent/partial/overpay/cross-tenant/dup-ref-409, +3 webhook/receipt) + adapter units (mock + HMAC). Browser QA passed (record cash → balance ₦40,000 → public receipt; online init → webhook reconcile → idempotent dup → bad-sig 401). Reviews fixed: fail-open→fail-closed prod guard + timing-safe mock sig (security hook), dup-offline-ref 409. Added invoice `id` to `getInvoice`/`InvoiceDetail`. Spec+plan `docs/superpowers/{specs,plans}/2026-06-16-sprint-4-slice-2-*`.
- **Known follow-ups (slice 2):** provider-reported amount is not reconciled against `payment.amountKobo` (log a mismatch warning later); public receipt formats NGN-only (no currency in the public payload); the webhook/RLS prod story still needs the `app.current_school_id` GUC wiring (a pre-deploy task across slices 5 + this).
- Sprint 4 slice 1 (2026-06-16): fee structure + invoicing (bursar/proprietor side, no payments yet). New `apps/api/src/modules/fees/` + 3 tenant-scoped models (TENANT_MODELS + RLS FORCE + 2 migrations): `FeeItem` (per classLevel+term named line items, kobo), `Invoice` (per student+term, `totalKobo`+`paidKobo`@0, balance derived), `InvoiceLine` (snapshot of fee items — frozen bill). `fees.service`: `setFeeItems` (replace-as-unit per classLevel+term, `fees.manage`), `generateInvoices(termId)` (`fees.manage`) — per enrolled student resolve classLevel via enrollment→class→classLevel → upsert Invoice by (studentId,termId) + snapshot lines + total, all in ONE `$transaction` (paid-state read + counters INSIDE the txn for TOCTOU/retry safety), **idempotent** (skips invoices with paidKobo>0, refreshes unpaid, never duplicates); `getInvoices`/`getInvoice` (`fees.view`). Money = kobo Int end-to-end; web `formatMoney(kobo, currency)` (vitest 3) formats at the edge via `School.currency` (₦ for NGN). Web: Settings → Fees (structure editor) + `/fees` bursar page (generate + balances table + invoice drill-in) + nav (Wallet). 116 API e2e (full suite, 16 files; +6 fees incl. replace-as-unit/totals/idempotent/skip-paid/**freeze**/cross-tenant) + formatMoney unit (3). Browser QA passed (configure JSS1 fees → generate → Ada+Bola ₦60,000 balances → drill-in Tuition+Levy; Settings loads existing structure). Code-review fixes: moved paid-state read + counters inside the generation `$transaction`. `fees.view`/`fees.manage` were already seeded + proprietor-auto-granted. Spec+plan `docs/superpowers/{specs,plans}/2026-06-16-sprint-4-slice-1-*`.
- Sprint 3 slice 6 (2026-06-16): results reveal animation (pure web, framer-motion added to apps/web). Button-triggered, screen-only reveal on `/report-card/[studentId]`: a `ResultReveal` overlay wraps the slice-5 static card in a `relative` container; phases `idle` (CTA) → `suspense` → `headline` (overall average count-up + position pop + DIY framer-motion celebratory burst for top performers) → `done` (overlay fades, exposing the card). **Print-safe by construction:** ALL motion is in a `print:hidden` overlay; the static card never gets motion/opacity, so Save-as-PDF is always complete at any phase. Pure `reveal.util.ts` (`shouldCelebrate` = position 1 OR avg ≥ top grade band; `topBandMinScore`) — vitest 6. `useReducedMotion()` short-circuits straight to `done`. Browser QA: celebrate path verified end-to-end (idle CTA → suspense → 84→85 count-up + "Position 1 of 2 🎉" → overlay exit → full card), card present in DOM at idle (print-safe). QA caveats: both seeded QA students are now distinction-level (slice-4.5 corrections raised Bola to 82), so the no-celebrate render wasn't visually exercised (logic unit-tested); `prefers-reduced-motion` couldn't be toggled via the QA tool (CDP media-emulation not allowlisted) — verified by code inspection (trivial branch). Spec+plan `docs/superpowers/{specs,plans}/2026-06-15-sprint-3-slice-6-*`.
- Sprint 3 slice 5 (2026-06-15): report-card PDF (print page) + public result verification. New **non-tenant** `Verification` model (NOT in TENANT_MODELS, NO RLS — the public path has no tenant context, so reading an RLS table would return nothing in prod) holding a per-`ResultSheet` minimal public snapshot keyed by a 16-char crypto `code` (`verification.util.generateVerificationCode`, Crockford-ish alphabet no 0/1/I/L/O). Created at release (`release.service`), refreshed on correction (`correction.service` re-rank loop upserts every affected sheet's snapshot), lazily created by the report-card endpoint for pre-slice-5 sheets. `report-card.service` (`GET /v1/assessment/report-card?studentId=&termId=`, `results.release`, tenant-scoped + IDOR) returns the frozen sheet + grade key + class size + the code. **First public endpoint:** `PublicModule` → `GET /v1/public/verify/:code` (NO guard, NO tenant) reads only `Verification` → `{valid, student, className, term, school, average, position, issuedAt}` (minimal; no entries/ids). Web: `/report-card/[studentId]?termId=` print page (letterhead + subject table + grade key + QR via `qrcode` + Save-as-PDF) + public `/verify/[code]` page (outside the `(app)` shell) + `Report card` entry per student on `/release`; `(app)` sidebar/topbar now `print:hidden`. 110 API e2e (full suite, 15 files; +1 release-Verification, +1 correction-refresh, +2 report-card incl. cross-tenant + idempotent, +2 public verify) + `generateVerificationCode` unit (2). Browser QA passed: report card renders + lazy code, public verify works with NO auth header (minimal fields, idempotent), bad code → not-found, page is standalone. Also fixed slice-4.5 OTP test isolation (reset `OtpRequest` in correction `beforeAll`). Spec+plan `docs/superpowers/{specs,plans}/2026-06-15-sprint-3-slice-5-*`.
- **Known follow-ups (slice 5):** the public `/v1/public/verify/:code` has no rate-limit/throttle (protected only by the unguessable code) — add throttling before prod; confirm prod CORS allows the public route for QR-scan contexts; optional school-logo letterhead.
- Sprint 3 slice 4.5 (2026-06-15): proprietor-signed correction of a released component score. New tenant-scoped `Correction` model (TENANT_MODELS + RLS FORCE + 2 migrations) capturing old/new value+total+position, reason, `otpVerified`, correctedBy. New `results.correct` permission (PROPRIETOR auto-grant; **backfilled** for existing proprietors). `School.requireCorrectionOtp Boolean @default(true)` — tenant-configurable step-up OTP. `AuthService.assertOtp(phone,code)` = single-use step-up re-verify (no JWT). `correction.service`: `correct()` (`results.correct`) → OTP gate when enabled + reason + value-range + tenant/release/IDOR validation → `$transaction` upserts the Score, recomputes the subject entry (create if previously unscored) + the student average + **re-ranks the whole class** (`computePositions`) + writes the `Correction`; `getConfig`/`setConfig` (`assessment.configure`) for the OTP flag; `getCorrectableScores` for the modal. Web: correction modal on `/release` (subject→component→value+reason+conditional OTP) + Settings → Assessment "Require OTP for corrections" toggle + api client. 104 API e2e (full suite; +6 correction incl. re-rank/audit/OTP-invalid/replay/reason/range/unreleased/cross-tenant, +3 config-flip/no-OTP/correctable, +2 assertOtp) . Browser QA passed BOTH modes (OTP-signed: Bola Exam→65, otpVerified=true, re-froze sheet; toggle off → no OTP field → CA2→9, otpVerified=false). Code-review fixes: truthful `otpVerified`, `schoolId`-scoped tx updates, dropped dead release check. Spec+plan `docs/superpowers/{specs,plans}/2026-06-15-sprint-3-slice-4_5-*`.
- **Known follow-up (slice 4.5):** OTP step-up is not user/purpose-bound (`OtpRequest` has no `userId`) — a login OTP for the same phone could be replayed as a correction OTP within its 10-min TTL. Phone is `@unique` + JWT-derived so cross-user is impossible; close fully later with a `userId`/`purpose` column on `OtpRequest`.
- Sprint 3 slice 4 (2026-06-15): release workflow + immutability. New models `Release`/`ResultSheet`/`ResultSheetEntry` (tenant-scoped: TENANT_MODELS + RLS FORCE + 2 migrations) — freeze-never-recompute. `release.service` (`results.release`): `release(classId,termId)` validates tenant ids → 409 if already released → gathers offered subjects + enrolled students + Scores → `computeSubjectResult` per (student,subject) → average = round(mean of scored offered-subject totals) → `computePositions` (pure helper, standard competition ranking 1-2-2-4) → one interactive `$transaction` creating Release + ResultSheet + entries (explicit `schoolId` on every create, since `tx` runs no middleware); `getStatus(termId)` (classes-with-enrolments + released flag, tenant-scoped via Class relation) + `getSheet(classId,termId)` (frozen rows ordered by position). **Immutability:** `ScoresService.saveScores` after `assertContext` → if a Release exists for (class,term,school) → 409 "Results released…". Web `/release` dashboard (term picker → per-class Release/View + frozen sheet table) + nav (Lock icon) + api client. Full API e2e = 14 suites / 93 tests (assessment suite +5: 4 release incl. tie + re-release 409 + status + cross-tenant 404, +1 immutability 409) + `computePositions` unit (3). Browser QA passed end-to-end (release → status flip → frozen sheet Ada P1 85/A1, Bola P2 8/F9 → gradebook edit blocked with surfaced 409). Code-review fixes applied: added missing `schoolId` scope on `subjectAssignment` read, closed `getStatus` cross-tenant over-read, added query-param guards. Spec+plan `docs/superpowers/{specs,plans}/2026-06-15-sprint-3-slice-4-*`.
- Sprint 3 slice 3 (2026-06-14): `anomaly.util` (flagAnomalies — population mean/σ/z, guards n<2 & σ=0) + `review` service (`GET /v1/assessment/review/class-master` + `subject-master`, `results.review`, compute-on-read, explicit scoping + IDOR) — class-master = students×subjects matrix (total/grade/anomaly + average); subject-master = per-class mean+drift vs subject mean/σ + per-student z/anomaly; anomaly cohort = per (subject,term) all classes. Web `/review` two-mode page. No new model; position still deferred to slice 4. 25 assessment e2e (+3 review incl. cross-tenant + deterministic >2σ) + anomaly.util unit. Browser QA passed (OUT z=−2.3 flagged, JSS2A drift +7.1 / JSS2B −10.7, API-matched). Spec+plan `docs/superpowers/{specs,plans}/2026-06-14-sprint-3-slice-3-*`.
- Sprint 3 slice 2 (2026-06-14): `Score` model (tenant + RLS) + `computeSubjectResult` (additive total + grade) + scores service (gradebook `GET /v1/assessment/scores` + batch `POST`, `results.record`, explicit schoolId scoping + value/enrollment/IDOR validation) + **structure guard** (block `PUT /assessment/types` once any score exists → 409). Web `/gradebook` grid (class/term/subject selectors, live total+grade per row, out-of-range flagged + save-blocked, empty states) + api client + `computeRow`. 19 assessment e2e (incl. cross-tenant + guard) + 22 web tests. Final review READY TO MERGE; browser QA passed (enter→live calc→save→persist; out-of-range blocked). Spec+plan `docs/superpowers/{specs,plans}/2026-06-14-sprint-3-slice-2-*`.
- Slice-2 follow-ups (non-blocking): blanking a saved cell doesn't clear it (omitted from batch, not zeroed) — decide delete-on-blank semantics in a later slice; `POST /scores` accepts empty `scores:[]` (no-op).
- Sprint 3 slice 1 (2026-06-14): new `apps/api/src/modules/assessment/` module — 3 tenant-scoped models (`AssessmentType`, `GradeBoundary`, `SubjectAssignment`) + RLS + `assessment.configure` permission; 3 API resources: assessment types (replace-as-unit, **sum-to-100**), grade boundaries (replace-as-unit + WAEC/NECO templates), subject assignments (per-year CRUD with tenant-IDOR validation); pure `resolveGrade`. Web: Settings → Assessment 3-panel config UI (grade bands w/ live preview, components w/ running-total gate, assignments w/ per-row teacher reassignment) + api client + `grade.ts` helper. 14 assessment e2e (incl. cross-tenant isolation) + 2 web grade tests. Final review: READY TO MERGE. Browser QA: all panels verified end-to-end (WAEC apply+save, types 100✓+save, assign/reassign/dup-409/remove). Spec+plan in `docs/superpowers/{specs,plans}/2026-06-14-sprint-3-slice-1-*`.
- **Sprint 3 (Assessment & Grading) COMPLETE** — slices 1–6 all shipped. (Deferred items, not blocking: full unlock-window correction — slice 4.5 shipped targeted single-score correction only; report-card public-verify rate-limit/CORS hardening + logo letterhead — slice 5; no-celebrate reveal render not visually QA'd + reduced-motion verified by inspection — slice 6.)
- Sprint 2 browser QA (2026-06-13): fixed 1 high-sev seam bug — overview rate showed "1.0%" instead of "100%" (API `rate` is a 0–1 fraction; web treated it as 0–100). Fix `1bb3a83`.
- Sprint 2.5 (2026-06-14): offline-first attendance. New `apps/web/src/lib/offline/` layer — `idb`-backed mark `queue` (composite-key coalescing) + `roster-cache` + pure `overlay` + `syncer` (batch-by-(class,date) flush, backoff retry, online/offline listeners) + `useOfflineSync` hook; grid (`/attendance`) rewritten offline-first (every tap durably queued, optimistic UI, cache-fallback load + queued-mark overlay, offline pill + sync indicator). Replay-safe via the existing idempotent `(studentId,date)` upsert — **zero backend change**. Offline browser QA verified the full round trip (offline mark → no premature send → reconnect sync → server persisted) + cache-fallback render with API down. Spec/plan in `docs/superpowers/{specs,plans}/2026-06-14-*`.
- **apps/web has vitest** (added Sprint 2.5): **22 web tests** (18 offline + 2 grade + 2 gradebook). API e2e: 22-test assessment suite (config + scores + guard + review + cross-tenant) + anomaly.util/score.util/grade.util units. All builds + typecheck + lint green.
- **Tenancy learning (important, applied in the assessment module):** the Prisma `$use` middleware does NOT reliably inject the `schoolId` filter inside an array `$transaction` NOR in the service-level e2e/`TenantContext.run` pattern. **Explicitly scope every read/delete by `TenantContext.schoolIdOrThrow()` + `where:{schoolId}`** in services; don't trust the middleware for these. (Single-record `update`/`delete` by unique id stay scoped via a prior scoped `findFirst` + RLS, per `prisma.service.ts`.) Existing modules rely on the middleware and their tests pass, but new tenant-scoped services should scope explicitly. [[tenant-idor-rule]]
- Security audit: `pnpm audit --prod` → 0 critical, 6 high (all transitive `tar` via bcrypt build toolchain, runtime-unreachable, unfixable upstream). CI gates on critical.
- **Open follow-ups (non-blocking):** (a) wrap `db.close()` in try/finally in `queue.ts`/`roster-cache.ts` (handle-leak on a rejected put/get; low impact); (b) Service Worker Background Sync for sync-while-closed (deferred — iOS Safari unsupported); (c) wrap the offline `Badge` pill in an `aria-live` region.

## What's built (by area)
- Auth: OTP request/verify, JWT, tenant middleware (verifies JWT), PermissionGuard, tokenVersion revocation.
- SIS: schools onboarding (create→proprietor grant+fresh token), academic years/terms, class levels, classes, subjects; students (+profile, photo upload w/ signed URLs), staff, parents/guardians, enrollment.
- Bulk import: BullMQ worker (tenant-scoped, row-level errors) + web CSV/XLSX UI (papaparse + exceljs).
- Attendance (Sprint 2): AttendanceRecord (tenant + RLS), roster/mark(batch upsert, last-write-wins, idempotencyKey)/student-history/summary; web `/attendance` tap-to-cycle grid + `/attendance/overview` heatmap. Hardened against cross-tenant IDOR.
- Offline attendance (Sprint 2.5): `apps/web/src/lib/offline/` — IndexedDB queue/cache + syncer + hook; grid is offline-first (durable queue, cache-fallback read, reconnect sync). Client-only; backend unchanged.
- Assessment (Sprint 3 s1–s5): `apps/api/src/modules/assessment/` — config + `Score` + gradebook + `computeSubjectResult` + structure guard + `anomaly.util` + review service + release workflow (`Release`/`ResultSheet`/`ResultSheetEntry` + `computePositions` + immutability) + proprietor-signed correction (`Correction` + step-up OTP `assertOtp` + tenant `requireCorrectionOtp` + re-rank) + report-card read + non-tenant `Verification`. `apps/api/src/modules/public/` — first unauthenticated endpoint (`/v1/public/verify/:code`). Web: Settings → Assessment, `/gradebook`, `/review`, `/release` (+ correction modal + report-card link), `/report-card/[studentId]` print page (+ `ResultReveal` button-triggered reveal animation), public `/verify/[code]`.
- Fees & Payments (Sprint 4 s1–s2): `apps/api/src/modules/fees/` (FeeItem/Invoice/InvoiceLine + structure config + idempotent invoice generation + balances) + `apps/api/src/modules/payments/` (`Payment` + `Receipt`, record/initialize/verify + idempotent reconcile) + `apps/api/src/core/payments/` (mock+Paystack provider) + public webhook/receipt routes. Web: Settings → Fees, `/fees` (finance summary + collections + balances + payment actions + bank-CSV reconcile), public `/receipt/[code]`. Money = kobo; `formatMoney` at the web edge. Fees s3 = collections (3a overdue/reminders) + 3b bank-CSV reconciliation + 3c finance summary. Fees s4 = parent self-serve: 4a parent identity link (auto-link PENDING→PARENT at OTP, `fees.pay.own`, `/v1/parent/children`) + 4b parent pay portal (`apps/api/src/modules/parent/` child-ownership-gated invoices/pay/verify delegating to PaymentsService; web role-aware nav + `/parent` portal + dashboard redirect).
- Reporting & Dashboards (Sprint 5, COMPLETE): `apps/api/src/modules/dashboard/` — `getProprietorSummary` (school-wide KPIs) + `getPrincipalSummary` (per-class table, batched) + `getAlerts` (three per-class trend signals) + `GET /v1/dashboard/{proprietor,principal,alerts}` (`reports.view`) + pure `attendanceRate`/`pickTopClass`/`feePaidRate`/`buildAlerts`. Web: role-aware `/dashboard` — proprietor showpiece (`ProprietorDashboardView`), non-proprietor staff get the principal operational table (`PrincipalDashboardView`, 403→stub fallback), and an `<AlertsPanel/>` on both.
- Web: `(app)` shell + sidebar (Dashboard, Students, Staff, Classes, Attendance, Gradebook, Review, Release, Fees, Settings), login, onboarding wizard. `/dashboard` is role-aware: PROPRIETOR → KPI showpiece; other staff → quick-links stub; PARENT → `/parent`.

## Next steps (in order)
1. **Sprint 6 — Communication / Announcements** (the remaining MVP module per PRD §4.8): broadcast announcements (school/class/level audience) → in-app inbox + SMS fallback + read receipts; later direct messaging (parent ↔ form teacher). Introduces `Announcement` (+ likely `DeliveryReceipt`) models + recipient resolution (reuse the Guardian fan-out from fee reminders) + a delivery pipeline (reuse SmsService/EmailService). Brainstorm → spec → plan → build, same disciplined workflow. **Sprint 5 is now complete** (s3 merged).
- Deferred (PRD Phase 2, when Sprint 6 done): Command-K palette, Ministry-format termly returns export, multi-school/trend dashboards, AI-narrative alert correlation.
2. **Deferred — `results.view.own` parent results view** (a later slice; Sprint 4 shipped fees/pay only). Other deferred follow-ups still open: public-verify + webhook throttling before deploy; the `app.current_school_id` RLS GUC wiring (pre-deploy); slice-4.5 unlock-window correction; payment provider-amount mismatch warning; parent auto-link phone-reassignment risk (accepted; could add invite/expiry later).
3. **Clear standing follow-ups:** Sprint 2.5 — db.close try/finally, SW Background Sync, offline-pill aria-live. Sprint 3 slice 1 — release note: backfill `assessment.configure` for any PROPRIETOR created before this deploy (the auto-grant only fires at school creation).

### Browser-QA playbook (reusable; learned this pass)
- Seed via API (no web enrollment screen): `POST /auth/otp/request {phone}` → OTP prints in api.log mock SMS → `POST /auth/otp/verify {phone,code}` → `POST /v1/schools` (use the returned **fresh token** thereafter) → `/v1/academic-years` (term `isCurrent:true`; roster resolves term by the `isCurrent` flag, NOT by date, so calendar drift is fine) → `/v1/class-levels` → `/v1/classes` → `/v1/students` ×3 → `/v1/enrollments` {studentId,classId,termId}.
- **gstack browse on this Windows box restarts the daemon between separate Bash calls, wiping localStorage** → do each interaction sequence in ONE bash call. Web stores auth in `localStorage` keys `mm.token` + `mm.user`; re-inject them at the start of each call (capture once after a real login), then `goto`.
- React controlled inputs: set value via the native setter + dispatch `input`+`change` (plain `fill`/`.value=` doesn't trigger React state). Set dependent date inputs one at a time with a wait between (batching otherwise drops one).
- `browse screenshot`/`snapshot -o` saves relative to the daemon's cwd (often `apps/web/.gstack/...`) — `find` it and copy into the repo, and `rm -rf apps/web/.gstack` after.
- **`next build` poisons `next dev`'s `.next`:** running `pnpm --filter web build` while/before `next dev` uses the same `.next` makes the dev server serve HTML referencing chunks that 404 → blank white pages app-wide. Fix: stop dev, `rm -rf apps/web/.next`, restart `next dev`. During QA, stop the web dev server before any production `build`.
- `browse js` does NOT await promises (async IIFEs return undefined) — can't read IndexedDB directly; use the UI's own indicators (e.g. the sync `pendingCount`) as the observable proxy. To emulate offline without a network toggle: `Object.defineProperty(navigator,'onLine',{value:false,configurable:true})` + `dispatchEvent(new Event('offline'))` (and `'online'` to restore); to test real fetch failure, kill the API process (taskkill the :4080 PID).

## How the work has been run
Orchestrator builds the tenancy/security-critical core inline (TDD); parallel subagents build disjoint modules/screens; orchestrator owns integration (barrels, app.module wiring) + verification + commits. Every commit triggers a background security-review hook — address its findings. Three independent safety nets used: automated tests, the security-review hook, and live browser QA (each catches a different bug class).

## Key learnings (also in agent memory)
- **Tenant IDOR rule:** validate any request-supplied entity id through a tenant-scoped model before write/return — especially `upsert` (middleware doesn't scope its where) and models without a `schoolId` (Enrollment, Guardian). Recurred 3×.
- Dependency policy: latest stable + `pnpm audit`; majors as a verified pass (test suite as guardrail), not blind bumps.
- Design: Bold Ink + Saffron, tokens in `packages/ui/tokens.ts` are the single source of truth.
